import test from 'node:test';import assert from 'node:assert/strict';import {assertSensoryPacket,createSensoryPacket,sanitizeMotorPacket,WORLD_TO_BRAIN_FIELDS} from '../src/core/protocol.js';
test('strict sensory API contains no world-coordinate or target fields',()=>{for(const forbidden of ['x','y','position','heading','objects','target','desiredHeading','nearest','bearing'])assert.equal(WORLD_TO_BRAIN_FIELDS.includes(forbidden),false);});
test('epistemic boundary rejects privileged state',()=>{assert.throws(()=>assertSensoryPacket({retinaBrightness:[],foodPosition:[1,2]}),/violation/);assert.throws(()=>assertSensoryPacket({objects:[]}),/violation/);});
test('sensory and neural evidence packets are bounded and explicit',()=>{const p=createSensoryPacket({retinaBrightness:[0],retinaMotion:[0],retinaLoom:[0],retinaProximity:[0],odorLeft:[0,0,0],odorRight:[0,0,0],touch:[0],taste:[0],airflow:[0],temperature:.5,proprioception:[0],metabolic:[0],memoryCue:[0],ambientNoise:0,dtMs:50});assert.equal(p.temperature,.5);const m=sanitizeMotorPacket({forward:9,reverse:-1,turn:-7,feed:2,drink:NaN,escape:0,odorBias:4,visualRisk:3});assert.equal(m.forward,1);assert.equal(m.reverse,0);assert.equal(m.turn,-1);assert.equal(m.feed,1);assert.equal(m.drink,0);assert.equal(m.odorBias,1);assert.equal(m.visualRisk,1);});
